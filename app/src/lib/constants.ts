/** HUMA's universal easing curve: cubic-bezier(0.22, 1, 0.36, 1) */
export const HUMA_EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];

/** Maximum characters allowed in a single chat message (enforced client + server). */
export const MAX_MESSAGE_LENGTH = 50_000;
