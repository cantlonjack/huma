# HUMA Current State

_Auto-generated by CC at end of session. The strategic Claude instance reads this._
_Last updated: 2026-04-05 (Session 60)_

## Routes

| Route | Status | Notes |
|-------|--------|-------|
| `/` | Working | Landing page. Redirects authed users with aspirations to `/today`. |
| `/start` | Working | **Step 0: archetype selection** before conversation. 9 domain archetypes in responsive grid (2-col mobile, 3-col desktop) + 3 orientation archetypes in "How you move" row. Multi-select, "Continue" (amber-600) appears after 1+ selection. "Skip — just talk" bypasses. Skips to conversation if archetypes already exist. **Session 58: Template pre-population.** After archetype selection, "Continue" leads to a template/blank fork screen: "Your starting shape" with WholeMiniPreview (120px static SVG silhouette showing membrane + aspiration nodes from selected templates). Two buttons: "Start with suggestions" (amber-600, pre-populates aspirations, patterns, and context hints from archetype templates) and "Start blank" (sage outline, saves archetypes only). Pre-populated aspirations marked `source: "template"` for identification. Optional collapsible "Sketch where things stand" section with 8 dimension sliders (saves to `known_context.capitalSketch`). Editability promise text below buttons. **Session 59: Archetype-aware conversation opening.** Instead of blank "What's going on?", HUMA speaks first based on selected archetype. Template path: reflects back starter aspiration names ("I've started with a couple of things..."). Blank-slate path: archetype-specific opener. Skip path: unchanged. Opening messages generated from `lib/archetype-openers.ts`. Template-sourced behaviors show "suggested" badge (italic, earth-300) in DecompositionPreview. `[[REPLACE_ASPIRATION:"..."]]` marker processed client-side to swap template aspirations with conversation-derived ones. Conversation entry. Deep context gathering, reflect-back with method intelligence, phased decomposition. Now auto-extracts patterns from trigger behaviors. |
| `/today` | Working | Production sheet. **Compiled sheet** wired: calls `compileSheet()` on load, renders action-level entries with through-line header. Cormorant Garamond headlines, Source Sans 3 detail inline, sand-100 bg + amber-600 left border on trigger item, generous vertical spacing (Alexander's Void). **Session 29 check-off redesign:** No checkbox circles — tap row to check off, text shifts to ink-200 with thin 1px line-through, dimension dots briefly glow then fade to 35%. No connector lines. Falls back to PatternRouteCards when compilation unavailable. Cached in localStorage by date. Dimension dots, streaks. WeekRhythm, cycle disruption detection. **Session 34:** "Notification settings" link visible for subscribed operators, opens NotificationSettings half-sheet. **Session 46:** COMING UP surfacing — when an aspiration's THIS WEEK behaviors hit 70%+ completion over 7 days, its first `comingUp` item appears below the sheet in a sage-bordered card. Shows aspiration name, timeframe, action name, and detail. Only after 7+ days of data. **Session 47:** Life stage transition detection — when 2+ aspirations decline 20+ percentage points simultaneously (28+ days of data, comparing two 14-day windows), a "Something shifted" card appears above the sheet. Ink-300 left border, sand-100 bg, Cormorant Garamond header. "Let's look at it" opens ChatSheet in reorganization mode. "Not now" dismisses with 14-day cooldown. |
| `/whole` | Working | Holonic life map. Force-directed D3 visualization with **behavioral correlation links**. **Session 60:** ArchetypeSelector redesigned — rich cards with descriptions, "Learn more" expand showing typical concerns/modifiers, sage checkmark on selection — aspiration nodes connected by sage-300 lines whose thickness and opacity reflect co-completion strength. Correlated holons drift closer via `forceLink`. Falls back to dimension overlap for new users. Archetype selector, WHY statement, insight card. ContextPortrait panel. **Session 43:** WHY evolution — after 28+ days, analyzes behavioral data vs original WHY, surfaces inline suggestion ("Your WHY is shifting") with observation, original, and evolved WHY. Operator can "Use this" or "Keep mine." Dismissal persisted per WHY cycle. **Session 44:** Insight thread annotations — top 3 delivered insights rendered as small italic Cormorant Garamond text near relevant holon nodes. Matched by dimension overlap. Museum placard aesthetic: ink-400, 6px, 75% opacity. **Session 48:** Shareworthy insight cards — when an insight involves 3+ dimensions, has ≥0.8 correlation, and 7+ data points, the InsightCard shows an amber "Share this" link. Tap opens full-screen letterpress card: dark bg (#1A1714), dimension circles with colors, Cormorant Garamond italic insight text, operator name, HUMA wordmark. Share via native share API or download as 1080×1080 PNG. **Session 49:** Living Canvas regeneration — below ContextPortrait, authenticated operators with aspirations see a "See your Living Canvas" prompt (or "Refresh" if already generated). Generates canvas from behavioral data via `/api/canvas-regenerate`. Displays essence phrase, CapitalRadar with computed scores, QoL nodes, production nodes, and closing sentence. Capital scores derived from dimension→capital mapping of actual behavior_log data (not self-reported). **Session 51:** Manage mode — gear icon in header toggles manage mode. In manage mode: aspiration and context nodes show × badges on the holon shape. Tapping any node opens HolonExpandPanel with danger zone: aspirations get Archive (sage outline) and Remove (rose text) buttons; context nodes get "Clear this" link; principles get Remove. All destructive actions go through ConfirmationSheet bottom sheet with HUMA-voice copy. After deletion, cached sheet is cleared to force recompile. **Session 53:** Context field management — ContextPortrait gains `manageMode` prop. In manage mode: each context section (Place, Work, Stage, Health, Time) shows a subtle × remove button next to the section label. People and resources lists show per-item × buttons for individual removal. Removal calls `removeContextField()` (Supabase + localStorage) with array index support (`people[2]`, `resources[0]`). "Add context" affordance: sage-outlined + circle button at bottom of ContextPortrait opens a dropdown menu of unpopulated field types. Selecting a type opens inline input (with detail field for Place/Work/Stage). Person and Resource always available to add more. **Session 54:** Fresh start / reset — sliders icon in header (adjacent to manage gear) opens SettingsSheet bottom sheet with three scoped reset options: "Clear chat history" (deletes chat_messages + localStorage chat keys), "Clear context" (resets known_context, WHY, archetypes — leaves aspirations), "Start fresh" (nuclear `clearAllUserData()` + `clearAllLocalStorage()`, redirects to `/start?fresh=1`). Two-step confirmation for each: first tap shows option list, second tap on confirmation sheet executes. HUMA-voice copy on all confirmation messages. |
| `/insight/[id]` | Working | Shareable insight page with OG metadata. Fetches insight from Supabase for dynamic OG image via `/api/og?insight=`. Immediately redirects to `/whole`. Exists solely for rich link previews when insights are shared. |
| `/grow` | Working | Patterns view. Patterns grouped by status (finding/working/validated). Cards show trigger with dimension pills, golden pathway, aggregate dimension row, validation progress, aspiration provenance, archetype badge. Tap-to-expand reveals coming_up and longer_arc phases. **Session 37:** Thin amber-600 SVG sparkline on each pattern card (14-day consistency). **Session 38:** "something changed" tappable link on dropping patterns opens ChatSheet investigation. **Session 39:** Pattern emergence detection — unnamed behaviors with 12+ of 14 days consistency appear at top as "Something forming..." in italic Cormorant Garamond on sand-100. Tap to reveal behavior name + consistency bar + dimensions. "Name it" opens inline input to formalize as a new pattern. "Not yet" dismisses (persisted in localStorage). Emerging behaviors excluded from patterns already tracked. **Session 40:** Cross-aspiration merge suggestions — when two patterns from different aspirations share behaviors, inline suggestion appears at bottom of pattern card. Operator can merge (combines golden pathways, deduplicates steps, deletes absorbed pattern) or keep separate (dismissal persisted in localStorage). **Session 41:** Breathing room — max 7 visible cards per section, overflow folds by primary dimension (expandable groups with dimension dot + count). Alexander's Void enforced: 20px card gaps, 32px section gaps, 24px page top/header padding. "Something forming..." header now in sand-100 rounded container (17px italic Cormorant Garamond). **Session 45:** Monthly review surface — auto-generated for previous calendar month. 4-column week grid (Mon–Sun) × behavior rows. Sage-100 = consistent (5+/week), amber-100 = intermittent (1–4/week), transparent = absent. Dimension dots on each row. Legend at bottom. Sorted by consistency (most consistent first). Appears below pattern sections. Requires auth + behavior_log data from prior month. **Session 52:** Pattern management — overflow menu (`···`, earth-400) on each pattern card with Edit/Archive/Remove options. **Edit** enters inline mode: pattern name, trigger, and golden pathway steps become editable inputs (sand F6F1E9 bg, DDD4C0 border); steps removable via × and addable via + Add step; time window editable; Save (amber-600) / Cancel (sand-200) buttons. **Archive** removes pattern from view with undo toast (5-second window, sage border, amber "Undo" link); deletes from Supabase, re-saves on undo. **Remove** opens ConfirmationSheet (reused from Whole page) with HUMA-voice copy. All actions persist to both Supabase and localStorage. Sheet cache cleared on archive/delete to force recompile. |
| `/chat` | Working | Conversation hub with grouped message cards and context summary. |
| `/map/[id]` | Working | Living Canvas renderer with OG metadata. |
| `/map/sample` | Working | Two example canvases. |
| `/api/v2-chat` | Working | Conversation engine with adaptive prompts, [[MARKER:...]] protocol, behavioral data feedback loop, and archetype/WHY identity context on all tabs. **Session 38:** Pattern investigation mode when `tabContext.selectedPattern` present with `trend: "dropping"` — system prompt shifts to investigative tone, asks what changed systemically, suggests revised decomposition if context shifted. **Session 46:** Financial context extraction — conversation phase prompt now explicitly instructs extraction of structured financial context (`[[CONTEXT:{"financial":{...}}]]`) when operator mentions money, budget, income, expenses, or debt. **Session 47:** Reorganization conversation mode — when `tabContext.transition` present with declining aspirations, system prompt switches to `REORGANIZATION_PROMPT`. Walks operator through what changed, suggests release/protect/revise for affected aspirations. Outputs `[[REORGANIZATION:{...}]]` marker with structured plan. **Session 59:** Method intelligence — reflect-back phase now evaluates whether operator's stated approach has a significantly better alternative (documented evidence, named practitioners, mechanical advantage). Surfaces during reflect-back only, with adjusted options. Template refinement — recognizes when operator contradicts template aspirations, outputs `[[REPLACE_ASPIRATION:"..."]]` marker. Template-awareness block injected when template-sourced aspirations exist. |
| `/api/chat` | Working | Legacy multi-phase conversation for canvas generation. |
| `/api/palette` | Working | Concept chip suggestions for /start sidebar. |
| `/api/sheet` | Working | Daily production sheet compilation via Claude. Action-level prompt. Returns `through_line` (structural connection sentence) alongside entries. Uses injected context: structured known_context, season, dayOfWeek, dayCount, archetypes, whyStatement, conversation transcript, 7-day history with analysis. **Session 30:** Hard 5-entry cap enforced server-side. When total behaviors > 5, leverage scoring pre-selects candidates by check-off momentum, day-of-week scheduling, and aspiration balance before sending to Claude. **Session 46:** Financial awareness — when `known_context.financial` is present, injects explicit FINANCIAL AWARENESS block into prompt with situation, pay rhythm, and constraints. Sheet headlines become cost-conscious (e.g. "$3.80 chicken stir-fry", "No spend day"). |
| `/api/sheet/check` | Working | Behavior check-off persistence. Requires auth. |
| `/api/insight` | Working | Cross-aspiration behavioral insights. Requires 7+ days of data. |
| `/api/whole-compute` | Working | Archetype and WHY statement generation. **Session 43:** `why-evolve` compute mode — takes original WHY + 28-day behavioral summary, returns evolved WHY suggestion with observation. |
| `/api/maps` | Working | Living Canvas storage (Supabase + Redis). |
| `/api/maps/[id]` | Working | Map retrieval (Supabase -> Redis fallback). |
| `/api/canvas-regenerate` | Working | Generates Living Canvas from operator's behavioral data. Auth required. Fetches aspirations, behavior_log (28 days), patterns, insights, known_context. Computes capital scores via dimension→capital mapping (`lib/capital-computation.ts`). Claude generates essence, QoL nodes, production nodes, closing from real data. Capital scores override LLM output. Stores result in `maps` table. |
| `/api/og` | Working | Dynamic OG image generation for maps AND insight cards. **Session 49:** New `?insight=` parameter renders letterpress-style insight card OG: dark bg (#1A1714), "HUMA SEES" label, dimension circles with colors, insight text in italic serif, operator name, HUMA wordmark. Fetches insight from Supabase REST API (edge-compatible). Falls back to generic HUMA OG. |
| `/api/push/subscribe` | Working | Stores web push subscription (PushSubscription JSON) to Supabase. Auth required. Upserts by user+endpoint. |
| `/api/cron/morning-sheet` | Working | Vercel cron (daily 12:00 UTC). Compiles sheet for each push-subscribed user, sends push with trigger item: "Your day: [headline]." Tap opens `/today`. Auth via `CRON_SECRET`. Respects `known_context.notifications.morning.enabled` — skips users who opted out. **Session 35:** ~2x/week, for users with 7+ days of data, appends a behavioral insight to the push. Checks for undelivered insight first, then generates via `/api/insight` (14-day correlation). First sentence only in push; full insight saved to DB. Response includes `insights` count. |
| `/api/reflection` | Working | Evening reflection processor. Takes reflection type (solid/shifted/hard) + optional text, sends to Claude for context extraction, merges updates into known_context, saves reflection as chat messages. Auth required. |
| `/auth/callback` | Working | Magic link auth callback. |

## Removed Routes

| Route | Removed In | Reason |
|-------|-----------|--------|
| `/system` | Session 14 | Function moves to Whole tab's expand panels (Phase 4). ConversationSheet component also removed. |

## Known Bugs

- None currently identified.
- **Evening reflection** requires at least 1 check-off in today's cached sheet (localStorage) and evening hour reached (default 5pm, configurable via notification preferences). Will not appear on fresh accounts or before evening.
- **Migrations required:** Run `009_aspiration_phases.sql`, `010_patterns.sql`, AND `011_push_subscriptions.sql` in Supabase. Code handles missing tables gracefully (push subscriptions won't persist until migration runs).
- **Env vars required for cron:** `CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY` must be set in Vercel. VAPID keys must also be configured (from Session 31).

## Last Session (Session 60 -- 2026-04-05)

### What Changed

1. **ArchetypeSelector redesign** (`components/whole/ArchetypeSelector.tsx`):
   - Replaced button grid (3×3 domain + row of 3 orientation) with rich card layout using archetype template data
   - Each card now shows: Cormorant Garamond 17px name, Source Sans 3 13px description, sage checkmark circle when selected
   - "Learn more →" toggle on each card expands to show typical concerns (domain) or modifier text (orientation)
   - Domain cards show "TYPICAL CONCERNS" header with italic concern list on expand
   - Orientation cards show italic modifier guidance on expand
   - Only one card expanded at a time (accordion behavior)
   - Cards sourced from `DOMAIN_TEMPLATES` and `ORIENTATION_TEMPLATES` in `archetype-templates.ts` — no more hardcoded name arrays
   - Bottom sheet max-height increased to 85vh to accommodate taller card layout
   - Design system compliant: sand-50 bg, sage borders on selected, EDE6D8 borders on unselected, amber-600 save button

### Previous Session (Session 59 -- 2026-04-05)

### What Changed

1. **Archetype-aware conversation opening** (`app/start/page.tsx`, `lib/archetype-openers.ts`):
   - New `lib/archetype-openers.ts` maps each of 9 domain archetypes to two opening messages: one for template path (references starter aspiration names) and one for blank-slate path (archetype-specific question)
   - When operator transitions from archetype selection to conversation, HUMA injects an opening message automatically instead of starting with a blank chat
   - Template path: "Creator — so the work matters. I've started with a couple of things that tend to come up: [names]. Tell me what's actually going on and we'll adjust."
   - Blank path: "Creator — so the work matters. What's the thing that keeps getting in the way of the making?"
   - Skip path: no opening message (unchanged)

2. **Method intelligence in reflect-back** (`api/v2-chat/route.ts`):
   - New instruction block in CONVERSATION_PHASE_PROMPT: when reflecting back, evaluate whether operator's stated method has a significantly better alternative with documented evidence, named practitioners, or mechanical advantage
   - Fires during reflect-back phase only (message 4+ or hard ceiling at 7), not during initial context gathering
   - When surfacing a better method, uses adjusted options: "Yes, show me behaviors" / "Tell me more about that" / "No, keep my approach"
   - Example: "You mentioned the static coop setup. Joel Salatin, Harvey Ussery — pasture rotation cuts feed costs 30%..."

3. **Template refinement** (`api/v2-chat/route.ts`, `lib/parse-markers-v2.ts`, `app/start/page.tsx`):
   - New `[[REPLACE_ASPIRATION:"..."]]` marker for when operator contradicts a template aspiration
   - Parser extracts the marker (simple string pattern like ASPIRATION_NAME)
   - Client-side handler in `/start` finds the first template-sourced aspiration and replaces it with conversation-derived text
   - Template-awareness block injected into system prompt when template-sourced aspirations exist

4. **"Suggested" badge on DecompositionPreview** (`components/DecompositionPreview.tsx`):
   - Template-sourced behaviors (with `source: "template"`) show a subtle "suggested" label (earth-300, italic, 11px) next to the behavior text
   - Label disappears when behavior is confirmed or replaced through conversation

5. **Behavior type update** (`types/v2.ts`):
   - Added optional `source: "template" | "conversation"` field to `Behavior` interface

6. **Template behavior source tagging** (`lib/archetype-templates.ts`):
   - `prePopulateFromArchetypes()` now sets `source: "template"` on individual behaviors (not just aspirations)

### Previous Session (Session 58 -- 2026-04-04)

### What Changed

1. **Step 0: Archetype selection on `/start`** (`app/start/page.tsx`):
   - New `ArchetypeSelectionScreen` component renders before conversation
   - Header: "What kind of life are you running?" (Cormorant Garamond 24px, earth-800)
   - 9 domain archetype cards in responsive grid (2-col mobile, 3-col desktop) using `ArchetypeCard` component from Session 56
   - 3 orientation archetype cards in "How you move" row with uppercase label (10px, earth-300)
   - Multi-select: toggles domain and orientation selections independently
   - "Continue" button (amber-600, full-width, rounded-full) — appears after 1+ selection with fade-in
   - "Skip — just talk" link (earth-400, 13px, underline) — bypasses archetype selection entirely
   - Editability promise: "This shapes your starting point. You can change it anytime from Whole." (sage-400, 12px) — flashes in/out over 1.8s on Continue
   - Saves selected archetypes to `localStorage["huma-v2-known-context"].archetypes` as string array
   - Skip-logic: if archetypes already exist in localStorage, or if messages exist (mid-conversation), skips to conversation step
   - `stepReady` flag prevents flash of wrong step during localStorage hydration

2. **CSS keyframe** (`globals.css`):
   - Added `promise-flash` keyframe animation for editability promise text

### Previous Session (Session 56 -- 2026-04-04)

### What Changed

1. **ArchetypeCard component** (`components/onboarding/ArchetypeCard.tsx`):
   - Rich visual card replacing plain text buttons for archetype selection
   - Cormorant Garamond 18px name, Source Sans 3 13px description, 11px italic typical concerns
   - Selected state: sage-50 bg, sage-400 border, subtle scale(1.02) with --huma-ease transition
   - Touch target minimum 52px height, sand-50 bg default, sand-200 border

2. **Archetype template data** (`lib/archetype-templates.ts`):
   - `DomainArchetypeTemplate` type: name, description, typicalConcerns, starterAspirations (with typed behaviors + dimensions), contextHints, relevantPatternIds
   - `OrientationArchetypeTemplate` type: name, description, modifier
   - All 9 domain archetypes with full template data: Earth Tender, Creator, Entrepreneur, Official, Economic Shaper, Spirit, Media, Educator, Parent
   - All 3 orientation archetypes with modifiers: Initiator, Manifestor, Destabilizer
   - Each domain template has 1-2 starter aspirations with concrete daily/weekly behaviors, dimension effects, context hints, and 4 relevant seed pattern IDs from HUMA_PATTERN_LIBRARY.md
   - Helper functions: `getDomainTemplate()`, `getOrientationTemplate()`
   - Exports: `DOMAIN_TEMPLATES`, `ORIENTATION_TEMPLATES`, `ARCHETYPE_TEMPLATES`

### Previous Session (Session 55 -- 2026-04-04)

### What Changed

1. **Empty states after deletion** — all three tab pages updated:
   - **Whole** (no aspirations): "Your shape starts here." (italic serif, sage-300) + "Start a conversation" amber link to `/start`
   - **Today** (no aspirations): "Nothing scheduled yet." + CTA button to `/start`
   - **Grow** (no patterns): "Patterns emerge from your aspirations." + "Start a conversation" amber link to `/start`

2. **Undo toast for archive on Whole page** (`app/whole/page.tsx`):
   - After archiving an aspiration, a toast appears at bottom: "[name] archived" + amber "Undo" link
   - 5-second auto-dismiss window. Undo restores aspiration to active in Supabase + localStorage + React state
   - Same visual treatment as Grow page toast: sand-100 bg, sage-200 border, slide-up animation
   - Data layer: `restoreLocalAspiration()` added to `lib/supabase-v2.ts` for localStorage undo

3. **Manage mode exit polish** (`app/whole/page.tsx`):
   - Toggling manage mode off now also dismisses any active archive toast

4. **Design system audit** — Phase A components:
   - Fixed 3 instances of pure white (`"white"`) backgrounds in `HolonExpandPanel.tsx` → sand-50 (`#FAF8F3`)
   - ConfirmationSheet, SettingsSheet, ContextPortrait manage mode all pass audit

5. **Sheet recompile triggers** — verified all deletion/archive paths (Whole: archive, delete, clear-context, delete-principle, settings reset; Grow: archive, remove) clear today's cached sheet to force recompile

### Previous Session (Session 54 -- 2026-04-04)

### What Changed

1. **SettingsSheet component** (`components/whole/SettingsSheet.tsx`):
   - New bottom sheet component with two-step flow: option list → confirmation
   - Three scoped reset options with icons, labels, and descriptions
   - "Clear chat history" — removes conversation history, keeps context/aspirations/patterns
   - "Clear context" — resets known_context, WHY, archetypes; leaves aspirations intact
   - "Start fresh" — nuclear reset, clears everything, redirects to `/start?fresh=1`
   - Confirmation step with HUMA-voice copy: "Go back" (sand) / action button (amber for partial, rose for nuclear)
   - Sand-50 bg, drag handle, slide-up animation (320ms, --huma-ease), backdrop dismiss
   - Design system compliant: Cormorant Garamond headers, Source Sans 3 body, sand/sage/amber palette

2. **Data layer additions** (`lib/supabase-v2.ts`):
   - `clearChatMessages(supabase, userId)` — deletes all `chat_messages` rows for user
   - `clearLocalChatMessages()` — removes `huma-v2-chat-messages` and `huma-v2-start-messages` from localStorage

3. **Whole page integration** (`app/whole/page.tsx`):
   - Sliders icon button added to header (adjacent to manage gear icon)
   - `settingsOpen` state controls SettingsSheet visibility
   - `handleSettingsAction` callback dispatches to appropriate data layer functions:
     - `clear-chat`: Supabase `clearChatMessages()` + `clearLocalChatMessages()`
     - `clear-context`: Supabase context reset (known_context: {}, why_statement: null) + `clearLocalStorageContext()` + React state reset
     - `start-fresh`: Supabase `clearAllUserData()` + `clearAllLocalStorage()` + redirect to `/start?fresh=1`
   - Cached sheet cleared after any reset to force recompile

### Previous Session (Session 53 -- 2026-04-04)

### What Changed

1. **Manage mode toggle** (`app/whole/page.tsx`):
   - Gear icon in Whole page header (sage-400, 20px SVG) toggles `manageMode` state
   - Active state: sage-50 background on icon, nodes show × badges
   - Exiting manage mode closes any open panel and clears pending confirmations

2. **WholeShape × badges** (`components/whole/WholeShape.tsx`):
   - New `manageMode` prop. When active, aspiration/context/principle nodes render a 12px amber-600 × badge at top-right
   - Identity node excluded (can't delete yourself)

3. **HolonExpandPanel danger zone** (`components/whole/HolonExpandPanel.tsx`):
   - New props: `manageMode`, `onArchive`, `onDelete`, `onClearContext`
   - Aspirations: "Archive" button (sage outline) + "Remove" button (rose-500 text) in a bordered footer section
   - Context nodes: "Clear this" link (earth-400 text)
   - Principles: "Remove" link (rose text)

4. **ConfirmationSheet** (`components/whole/ConfirmationSheet.tsx`):
   - New reusable bottom sheet for destructive actions
   - Slide-up animation (320ms, --huma-ease), sand-50 bg, rounded-t-2xl
   - HUMA-voice copy per action type: archive (recoverable), delete (permanent with cascade warning), clear context, remove principle
   - Two buttons: "Keep it" (sand-200) / action-specific confirm (rose-500)
   - Backdrop tap dismisses

5. **Delete/archive wiring** (`app/whole/page.tsx`):
   - Archive: calls `archiveAspiration()` + `archiveLocalAspiration()`, removes node from state
   - Delete: calls `deleteAspiration()` + `removeLocalAspiration()`, removes node from state
   - Clear context: calls `removeContextField()` + `removeLocalContextField()`, updates context state + localStorage
   - Delete principle: calls `deletePrinciple()`, removes from principles state
   - After any action: clears today's cached sheet (`huma-v2-sheet-YYYY-MM-DD`) to force recompile

6. **Chat context awareness**: contextPrompt updates to "What would you like to change?" when manage mode is active

### Previous Session (Session 50 -- 2026-04-03)

### What Changed

1. **Data layer: delete & archive functions** (`lib/supabase-v2.ts`):
   - `archiveAspiration()` — sets status to "archived" (soft delete, recoverable)
   - `deleteAspiration()` — hard delete with cascade: deletes related patterns, behavior_log rows, then the aspiration (sheet_entries cascade via FK)
   - `removeContextField()` — removes a key from known_context JSONB. Supports top-level keys ("place") and array index paths ("people[2]")
   - `clearAllUserData()` — nuclear reset: clears known_context, why_statement, deletes all rows from aspirations, patterns, sheet_entries, behavior_log, insights, principles, chat_messages
   - Aspiration status type updated to include "archived" (in `types/v2.ts`)

2. **localStorage CRUD helpers** (`lib/supabase-v2.ts`):
   - `removeLocalAspiration(id)` — removes aspiration + related patterns from localStorage
   - `archiveLocalAspiration(id)` — sets aspiration status to "archived" in localStorage
   - `removeLocalContextField(path)` — removes context field from localStorage (supports array syntax)
   - `clearLocalStorageContext()` — resets known_context to {} in localStorage
   - `clearAllLocalStorage()` — removes all huma-v2-* and huma-conversation keys
   - `removeLocalPattern(id)` — removes a pattern from localStorage

3. **deletePattern cascade verification**:
   - Confirmed: behavior_log has no FK to patterns — deletePattern() already clean, no orphans possible

### Previous Session (Session 49 -- 2026-04-03)

### What Changed

1. **Capital score computation** (`lib/capital-computation.ts`):
   - New module: computes 8 Forms of Capital (financial, material, living, social, intellectual, experiential, spiritual, cultural) from behavioral data
   - Maps HUMA's 8 dimensions to capital forms via primary/secondary contributions
   - Scores derived from completion rates × engagement factor → 1–5 scale
   - `computeCapitalScores()` and `dimensionActivityFromCounts()` exported for reuse

2. **Canvas regeneration API** (`app/api/canvas-regenerate/route.ts`):
   - POST endpoint (auth required) generates Living Canvas from operator's actual data
   - Fetches: aspirations, behavior_log (28-day window), patterns, insights, known_context
   - Computes capital scores from behavioral dimension mapping (not Claude-generated)
   - Claude generates essence phrase, QoL nodes, production nodes, closing
   - Capital scores override any LLM values — behavioral data is source of truth
   - Stores regenerated canvas in `maps` table via upsert

3. **OG route insight support** (`app/api/og/route.tsx`):
   - New `?insight=` parameter renders letterpress-style OG card
   - Dark background (#1A1714), "HUMA SEES" label, dimension circles with colors
   - Insight text in italic serif, operator name, HUMA wordmark footer
   - Fetches from Supabase REST API (edge-compatible)
   - Optional `?operator=` parameter for operator name on card

4. **CanvasRegenerate component** (`components/whole/CanvasRegenerate.tsx`):
   - New component on Whole page (below ContextPortrait, above InsightCard)
   - Shows for authenticated operators with aspirations
   - Idle state: dashed-border "See your Living Canvas" prompt
   - Generating state: spinner with "Reading your patterns..."
   - Done state: essence phrase, CapitalRadar (lazy-loaded, animated), QoL/production pills, closing
   - "Refresh" button to regenerate from updated behavioral data

5. **Shareable insight pages** (`app/insight/[id]/page.tsx`):
   - Server-rendered page with dynamic OG metadata for insight sharing
   - `generateMetadata()` fetches insight text + dimensions + operator name
   - OG image URL points to `/api/og?insight=[id]&operator=[name]`
   - Page immediately redirects to `/whole` — exists solely for rich link previews

6. **ShareworthyInsightCard updates** (`components/whole/ShareworthyInsightCard.tsx`):
   - Share URL now points to `/insight/[id]` (has proper OG metadata) instead of `/whole?insight=`
   - Native share includes both image file AND shareable URL
   - Link previews on social platforms show the letterpress-style insight card

### What Was Verified
- TypeScript: 0 errors
- Production build: clean, 25 routes generated (0 errors)

### Why This Matters (Roadmap)
Increment 6 Session 2: "Living Canvas evolution + share." The Living Canvas is no longer a one-time artifact from the onboarding conversation — it regenerates from the operator's actual behavioral data. Capital scores are COMPUTED from what the operator does (dimension→capital mapping), not self-reported or guessed. The share system extends beyond maps to insight cards: when an operator shares a strong cross-dimensional insight, the link generates a rich letterpress preview via `/api/og`. This completes Increment 6 (The Shareworthy Moment) and the V2.1 build roadmap (Sessions 25–49).

## Previous Session (Session 48 -- 2026-04-03)

### What Changed

1. **ShareworthyInsightCard component** (`components/whole/ShareworthyInsightCard.tsx`):
   - New full-screen letterpress-style card for strong cross-dimensional insights
   - Dark background (#1A1714) with subtle sage radial glow
   - "HUMA SEES" label, dimension circles (colored per DIMENSION_COLORS), thin divider
   - Insight text in italic Cormorant Garamond centered, operator name below
   - HUMA wordmark footer
   - Interactive card for display + hidden 1080×1080 export layout for image generation
   - Share via native Web Share API (with file), fallback to PNG download
   - `isShareworthyInsight()` helper exported: requires 3+ dimensions, ≥0.8 correlation, 7+ data points

2. **InsightCard updated** (`components/whole/InsightCard.tsx`):
   - New optional props: `shareworthy?: boolean`, `onShare?: () => void`
   - When shareworthy, shows amber "Share this" link next to "See full pattern →"
   - Actions row layout: space-between flex for both links

3. **Whole page integration** (`app/whole/page.tsx`):
   - Imports `ShareworthyInsightCard` and `isShareworthyInsight`
   - New `shareworthyOpen` state controls full-screen card visibility
   - InsightCard receives shareworthy flag and onShare callback
   - ShareworthyInsightCard rendered as portal-like overlay with operator name

4. **CSS animation** (`globals.css`):
   - New `insight-circle-in` keyframe: scale(0.6) → scale(1) with opacity, 600ms
   - Used for staggered dimension circle entrance in the shareworthy card

### What Was Verified
- TypeScript: 0 errors
- Production build: clean, all routes generated
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 5 Session 3: "Life stage transition detection." When an operator's life changes — new job, moved, relationship shift, health event — multiple patterns drop simultaneously. Instead of nagging about individual behaviors, HUMA detects the systemic signal and opens a reorganization conversation. The operator identifies what changed, then HUMA helps them release aspirations that no longer fit, protect ones that matter more now, and revise behaviors to match the new reality. This completes Increment 5 (Temporal Intelligence).

## Previous Session (Session 46 -- 2026-04-03)

### What Changed

1. **Financial context type** (`types/v2.ts`):
   - New `FinancialContext` interface: `situation`, `income`, `constraints[]`, `rhythm`
   - Added `financial?: FinancialContext` field to `KnownContext`

2. **Conversation engine financial extraction** (`app/api/v2-chat/route.ts`):
   - Conversation phase prompt now explicitly instructs extraction of structured financial context
   - When operator mentions money/budget/income/expenses/debt, extracts via `[[CONTEXT:{"financial":{...}}]]`

3. **Sheet compiler financial awareness** (`app/api/sheet/route.ts`):
   - `formatKnownContext()` now formats `financial` field as prose (situation, income, pay rhythm, constraints)
   - Added `"financial"` to known keys set (no longer falls through as unstructured)
   - New FINANCIAL AWARENESS prompt section injected when financial context present: instructs pay-rhythm timing, budget-respecting meal plans, specific dollar amounts in headlines

4. **COMING UP surfacing on Today page** (`app/today/page.tsx`):
   - New `getReadyComingUp()` function: computes aspiration-level 7-day completion rate from `weekCounts`, flags aspirations at 70%+ as "solid"
   - For solid aspirations with `comingUp` data, surfaces the first COMING UP item
   - New `ComingUpSection` component: sage-300 left border, sand-100 bg, aspiration name + timeframe label, Cormorant Garamond action name, Source Sans 3 detail
   - Rendered below compiled sheet entries / pattern route cards, above reroute card
   - Requires 7+ days of data (dayCount threshold)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 5 Session 2: "Financial rhythm + COMING UP surfacing." Two features that make the production sheet responsive to context the operator has shared. Financial context flows from conversation into known_context, then into the sheet prompt — so an operator who mentioned "$200/week groceries" starts seeing headlines like "$3.80 chicken stir-fry for two" instead of "Cook a healthy dinner." The COMING UP surfacing completes the decomposition loop: when THIS WEEK behaviors are consistently done (70%+ over 7 days), the Today page gently surfaces the next phase — the first comingUp item appears below the sheet, signaling "you've earned the right to expand."

## Previous Session (Session 45 -- 2026-04-03)

### What Changed

1. **Monthly review types** (`types/v2.ts`):
   - New `WeekConsistency` type: `"consistent" | "intermittent" | "absent"`
   - New `MonthlyReviewRow` interface: `behaviorKey`, `behaviorName`, `dimensions`, `weeks` (4 entries)
   - New `MonthlyReviewData` interface: `month` label, `weekRanges` labels, `rows`

2. **Monthly review data query** (`lib/supabase-v2.ts`):
   - New `getMonthlyReviewData()`: queries behavior_log for previous calendar month
   - `partitionMonthIntoWeeks()`: splits month into Mon–Sun week buckets (up to 4 displayed)
   - Consistency scoring: 5+ completions (or all days in partial week) = consistent, 1–4 = intermittent, 0 = absent
   - Enriches rows with dimension data from aspirations
   - Sorted by consistency (most active behaviors first)
   - Returns null if no behavior data exists for previous month

3. **MonthlyReview component** (`components/MonthlyReview.tsx`):
   - 4-column week grid × behavior rows in a sand-50 rounded container
   - Sage-100 cells with sage-200 border = consistent
   - Amber-100 cells with amber-400 border = intermittent
   - Transparent cells with sand-200 border = absent
   - Dimension dots (up to 3) next to each behavior name
   - Week range labels as column headers (e.g. "Mar 3–9")
   - Legend row at bottom: consistent / intermittent / absent
   - Cormorant Garamond month heading, Source Sans 3 body text

4. **Grow page integration** (`app/grow/page.tsx`):
   - `getMonthlyReviewData()` called in data loading effect (auth required)
   - MonthlyReview rendered below pattern sections with 32px top margin
   - Only appears when previous month has behavior_log data

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 5 Session 1: "Monthly review surface." The Grow tab has tracked daily patterns via sparklines — now it gains temporal depth. At the end of each month, a quiet grid appears showing the shape of the operator's behavior across four weeks. No numbers, no grades — just sage (you were there), amber (some days), or empty (absent). The operator sees their month as a field: which rows stayed green, which flickered, which went dark. It's the first surface that lets them see rhythm at the scale of a month, not just a fortnight. Combined with sparklines (14-day) and pattern status (30-day validation), the Grow tab now covers three temporal scales.

## Previous Session (Session 44 -- 2026-04-03)

### What Changed

1. **Behavioral summary query** (`lib/supabase-v2.ts`):
   - New `BehavioralSummary` interface: `totalDays`, `topBehaviors` (key, name, completedDays), `dimensionCounts`
   - `getBehavioralSummary()` queries `behavior_log` for 28-day window of completed behaviors
   - Groups by behavior_key, counts distinct active dates, resolves dimension counts from aspiration metadata
   - Returns top 10 behaviors by consistency — feeds the WHY evolution AI prompt

2. **WHY evolution API** (`app/api/whole-compute/route.ts`):
   - New `WHY_EVOLVE_SYSTEM` prompt: compares original WHY with behavioral data
   - `compute: "why-evolve"` mode takes `originalWhy` + `behavioralSummary` + `contextData`
   - Returns `{ evolved: boolean, evolvedWhy?: string, observation?: string }`
   - Honors the original — if the WHY still fits, `evolved: false` and no suggestion

3. **WhyEvolution component** (`components/whole/WhyEvolution.tsx`):
   - Inline suggestion card: "YOUR WHY IS SHIFTING" label in sage uppercase
   - Observation sentence describing what behavioral data reveals
   - Side-by-side: "You started with" (original, muted sage italic) vs "What you're actually building" (evolved, strong sage-700 italic)
   - Two buttons: "Use this" (sage-700 bg, sand-50 text) and "Keep mine" (outline)
   - HUMA design language: sand-50 bg, sage accent line, Cormorant Garamond, no bounce

4. **Whole page integration** (`app/whole/page.tsx`):
   - Loads `whyDate` from `getWhyStatement()` alongside existing data
   - After 28+ days: fetches behavioral summary, calls `why-evolve`, renders WhyEvolution below ProfileBanner
   - "Use this" updates WHY in Supabase + localStorage + ProfileBanner (new `why_date` set)
   - "Keep mine" dismisses and persists dismissal per WHY cycle in localStorage
   - Guard: requires 7+ active days of behavioral data to trigger

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 4 Session 2: "WHY statement evolution." The original WHY was a guess — drafted from context before the operator had lived with HUMA. After 4+ weeks of actual behavior, the system now knows what the operator is really building. The WHY evolution surfaces this gently: "Your strongest patterns all center on evening rituals with your family." Here's what you said. Here's what you're actually doing. You choose. This is recognition, not correction — the Whole page becomes a mirror that deepens over time. The operator's identity statement grows alongside their practice.

## Session 42 (2026-04-03)

### What Changed

1. **Aspiration behavioral correlation** (`lib/supabase-v2.ts`):
   - New `AspirationCorrelation` interface: `sourceId`, `targetId`, `weight` (0–1), `coCompletionDays`
   - `getAspirationCorrelations()` queries `sheet_entries` for the last 30 days
   - Groups checked entries by aspiration_id + date, computes pairwise co-completion
   - Normalizes by the smaller set size (Jaccard-like overlap)
   - Filters to meaningful correlations only: 3+ co-completion days, weight > 0.3

2. **Behavioral links in WholeShape** (`components/whole/WholeShape.tsx`):
   - New `HolonLink` type: `sourceId`, `targetId`, `weight`
   - `forceLink` added to D3 simulation — correlated aspirations drift closer
   - Link distance: 30px down to 15px as weight increases (gradual, not reactive)
   - Link strength: `weight × 0.3` — gentle pull that respects layer positioning
   - SVG `<line>` elements rendered behind nodes in sage-300 (#A8C4AA)
   - Thickness: 0.5px–2.0px proportional to weight
   - Opacity: 0.15–0.4 proportional to weight — subtle, not diagrammatic

3. **Dimension overlap fallback** (`app/whole/page.tsx`):
   - `computeDimensionLinks()` computes structural links from shared dimensions
   - Used when no behavioral data exists (pre-auth, new users, <3 days)
   - Threshold: 40% dimension overlap required to show a link
   - Behavioral correlations take precedence when available

4. **Data flow** (`app/whole/page.tsx`):
   - `getAspirationCorrelations()` fetched in parallel with existing Supabase queries
   - `holonLinks` computed via `useMemo` from correlations or dimension fallback
   - Passed to WholeShape as `links` prop

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 4 Session 1: "Behavioral weight on holon connections." The Whole page was a static map — nodes positioned by layer, but no visual signal about how parts of life connect through behavior. Now the holonic visualization shows the operator's system as it actually works: aspirations that get completed together drift closer and grow visible connections. The sage-300 lines are subtle — museum-quality, not dashboard-widget — thickness and opacity proportional to how strongly two parts of life co-activate. This is the first step toward the Whole page becoming a living mirror: the shape of the holon changes based on what the operator actually does, not just what they said they wanted.

## Session 41 (2026-04-03)

### What Changed

1. **Dimension folding** (`app/grow/page.tsx`):
   - `MAX_VISIBLE_CARDS = 7` — when a PatternSection has >7 patterns, first 5 render directly
   - Overflow patterns grouped by primary dimension via `groupByDimension()` helper
   - `DimensionFold` component: collapsible group with dimension dot, label, pattern count, chevron
   - Expands/collapses with HUMA easing (cubic-bezier 0.22, 1, 0.36, 1)
   - "N more by dimension" italic Cormorant Garamond label above folds

2. **Alexander's Void** (`app/grow/page.tsx`):
   - Card `marginBottom`: 16px → 20px
   - PatternSection `marginBottom`: 24px → 32px
   - Section header `marginBottom`: 12px → 16px
   - Page top padding: 16px → 24px
   - Page header bottom padding: 16px → 24px

3. **EmergenceCard refinement** (`components/EmergenceCard.tsx`):
   - "Something forming..." header now wrapped in sand-100 (#F6F1E9) rounded container
   - Font size: 16px → 17px, added explicit lineHeight 1.3
   - Section bottom margin: 24px → 32px, header margin: 12px → 16px

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 3 Session 6 (final): "Grow tab breathing room." The Grow tab was accumulating visual density as operators build more patterns — 10+ cards stacked vertically creates cognitive load, which is the opposite of what HUMA exists to do. Now the tab breathes. A maximum of 7 cards show per section; overflow folds by dimension, so an operator with 12 patterns sees the 5 most relevant directly and the rest organized by what part of their life they serve. The "Something forming..." emergence header gets a sand-100 container — warm, distinct, a visual pause before the pattern cards begin. Generous spacing (Alexander's Void) throughout: cards don't crowd each other, sections have room to be distinct, the page top has breathing room. This is the Christopher Alexander principle: the spaces between elements matter as much as the elements themselves. The Grow tab should feel like a field journal, not a task list.

## Previous Session (Session 40 -- 2026-04-03)

### What Changed

1. **MergeSuggestion type** (`types/v2.ts`):
   - New `MergeSuggestion` interface: `patternId`, `otherPatternId`, `otherPatternName`, `sharedBehaviors` (string[])
   - Represents a detected opportunity to merge two patterns that share behaviors across different aspirations

2. **detectMergeCandidates()** (`lib/supabase-v2.ts`):
   - Pure function — compares all patterns pairwise for shared step text (case-insensitive)
   - Only suggests merges across different aspirations (same-aspiration patterns stay separate)
   - Returns at most one suggestion per pattern (strongest overlap wins)
   - Both patterns in a pair get a suggestion pointing to the other

3. **mergePatterns()** (`lib/supabase-v2.ts`):
   - Keeps the primary pattern, absorbs non-trigger steps from the secondary
   - Deduplicates by step text (case-insensitive) — no duplicate behaviors in merged pathway
   - Reorders absorbed steps after existing primary steps
   - Updates primary in Supabase via `updatePattern()`, deletes secondary via `deletePattern()`
   - Returns the updated primary pattern for local state update

4. **Merge suggestion UI** (`app/grow/page.tsx`):
   - Inline banner at bottom of PatternCard (warm #FDFAF5 background, below provenance)
   - Single shared behavior: `"[behavior]" also lives in [other pattern name].`
   - Multiple shared behaviors: `N shared behaviors with [other pattern name].`
   - Two actions: "Merge" (amber-600 outlined button) and "Keep separate" (text-only sage-400)
   - `e.stopPropagation()` prevents card expand/collapse when interacting with merge UI

5. **Grow page state** (`app/grow/page.tsx`):
   - `mergeSuggestions` computed from current patterns via `detectMergeCandidates()`
   - Dismissed merges persisted in `huma-v2-dismissed-merges` localStorage key
   - `handleMerge`: calls `mergePatterns()` on Supabase, updates local patterns state (replaces primary, removes secondary)
   - `handleDismissMerge`: dismisses both directions of a suggestion pair
   - Merge props passed through PatternSection → PatternCard

### Why This Matters (Roadmap)
Increment 3 Session 5: "Cross-aspiration merging." When a behavior — say "morning walk" — appears in two different aspirations (one for body, one for mental clarity), HUMA now sees the connection. An inline suggestion surfaces on the pattern card: "'Morning walk' also lives in Clear Mornings." The operator can merge the patterns (combining their golden pathways into one richer sequence) or keep them separate (the connection is acknowledged but the patterns remain distinct). This is HUMA seeing the wholeness that the operator built naturally — behaviors that serve multiple dimensions aren't duplicates, they're convergence points. The merge preserves the primary pattern's trigger and validation progress while absorbing the secondary's unique steps. The secondary pattern is deleted. The system becomes simpler without losing anything. This is infrastructure seeing what the operator already knew: their life is one connected system.

## Previous Session (Session 39 -- 2026-04-03)

### What Changed

1. **EmergingBehavior type** (`types/v2.ts`):
   - New `EmergingBehavior` interface: `behaviorKey`, `behaviorName`, `completedDays`, `totalDays` (always 14), `dimensions` (from source aspiration), optional `aspirationId`
   - Represents unnamed behaviors that have appeared consistently (12+ of 14 days) without being part of an existing pattern

2. **detectEmergingBehaviors()** (`lib/supabase-v2.ts`):
   - Single query fetches all completed behavior_log rows in 14-day window
   - Groups by behavior_key, counts distinct completed dates
   - Filters to 12+ completed days AND not already in any existing pattern's steps
   - Enriches with dimension data from aspirations where behavior_key matches
   - Returns `EmergingBehavior[]`

3. **EmergenceCard component** (`components/EmergenceCard.tsx`):
   - "Something forming..." header in italic Cormorant Garamond (sage-500)
   - Sand-100 background cards, sand-200 border — warm emergence aesthetic
   - Collapsed: "A pattern is forming" with day count teaser
   - Tap to reveal: behavior name, consistency bar (sage-400 fill), dimensions touched
   - "Name it" button opens inline text input (Cormorant Garamond italic placeholder)
   - Enter or Save button formalizes the behavior into a new pattern
   - "Not yet" dismisses — persists dismissal in localStorage
   - Reveal animation: 300ms fade + translate with HUMA easing

4. **Grow page integration** (`app/grow/page.tsx`):
   - `emergingBehaviors` state loaded after sparklines in data effect
   - `detectEmergingBehaviors()` called with existing patterns + aspirations for filtering
   - Previously dismissed keys filtered via `huma-v2-dismissed-emergences` localStorage
   - EmergenceCard rendered above pattern sections (top of content)
   - Empty state now checks both `patterns.length === 0` AND `emergingBehaviors.length === 0`
   - `handleFormalize`: creates new Pattern with single trigger step, saves to Supabase + localStorage, adds to patterns state, removes from emerging
   - `handleDismissEmergence`: persists behaviorKey to dismissed list in localStorage, removes from state
   - Emerging behaviors added to tabContext for chat overlay awareness

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 3 Session 4: "Pattern emergence detection." Patterns in HUMA aren't just extracted from decompositions — they can emerge from living. When an operator checks off the same behavior 12 out of 14 days without it being part of any formal pattern, something is forming. The system notices and surfaces it quietly at the top of the Grow tab: "Something forming..." in italic serif on warm sand. Not a congratulation, not a streak count — an observation. Tap to see what it is. The operator can name it (formalizing it into a pattern with a single trigger step) or say "Not yet" (dismissing it until it surfaces again). This is the system seeing what the operator might not see about themselves — a behavior that has become consistent enough to be named. The card aesthetic is deliberately different from formal pattern cards: softer, warmer, more tentative. Something emerging from the ground, not yet fully formed.

## Previous Session (Session 38 -- 2026-04-03)

### What Changed

1. **"What changed?" tappable investigation** (`app/grow/page.tsx`):
   - "something changed" label on dropping patterns is now a `<button>` with dotted underline
   - `onInvestigate` callback flows: PatternCard → PatternSection → GrowPage
   - `investigatePatternId` state drives `forceOpen` on TabShell to open ChatSheet
   - `findDropOffDate()` helper walks sparkline data to find approximate decline point

2. **ChatSheet initial message support** (`components/ChatSheet.tsx`):
   - New `initialMessage` prop: pre-loaded HUMA opener injected after message history loads

3. **Pattern investigation system prompt** (`api/v2-chat/route.ts`):
   - When `tabContext.selectedPattern` present with `trend: "dropping"`, system prompt enters investigation mode
   - Investigative tone: curious not concerned, systemic not motivational
   - Can produce revised decompositions via [[DECOMPOSITION:...]] and capture new context via [[CONTEXT:...]]

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 3 Session 3: "What changed? conversation." When a pattern drops off, the operator sees "something changed" as a tappable link — an invitation. Tapping opens a conversation pre-loaded with the right question. The system looks at the system, never at the person.

## Previous Session (Session 37 -- 2026-04-03)

### What Changed

1. **Sparkline SVG component** (`components/Sparkline.tsx`):
   - New component: takes `SparklinePoint[]` + `trend`, renders a thin amber-600 polyline
   - 80×24px default size, configurable via props
   - Subtle gradient fill beneath the line for visual weight (12% opacity amber → transparent)
   - 1.5px strokeWidth, rounded caps and joins — feels hand-drawn, not data-dashboard
   - No labels, no axes, no numbers — the shape is the information
   - `aria-hidden="true"` — decorative, the trend label carries the accessible meaning

2. **PatternCard sparkline integration** (`app/grow/page.tsx`):
   - Sparkline sits to the right of the validation progress bar in the card footer
   - Progress bar flexes to fill remaining space; sparkline is fixed 80px width
   - Only renders when sparkline data has 2+ points (graceful absence for new patterns)
   - Trend label upgraded to `font-serif` (Cormorant Garamond italic) — matches the card's typographic hierarchy

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 3 Session 2: "Pattern card redesign + sparkline UI." Session 36 computed the data — 14 days of daily completion ratios for every pattern. This session makes it visible. A thin amber line on each pattern card shows the operator their consistency at a glance. Rising lines mean momentum is building. Flat lines mean the pattern is stable — it's part of the operating system now. Dropping lines mean something changed in the operator's context. The sparkline has no labels because it's not a chart — it's a pulse. The operator sees the shape and knows. The trend label ("momentum" or "something changed") is now in Cormorant Garamond italic, matching the card's typographic hierarchy — data speaks in the serif voice, not the UI voice.

## Previous Session (Session 36 -- 2026-04-03)

### What Changed

1. **SparklineData types** (`types/v2.ts`):
   - New `SparklinePoint` interface: `date` (YYYY-MM-DD) + `ratio` (0–1, completions / total steps)
   - New `SparklineData` interface: `patternId`, `points` (14 entries oldest-first), `trend` ("rising" | "stable" | "dropping")

2. **getPatternSparklines()** (`lib/supabase-v2.ts`):
   - Single DB query fetches all behavior_log rows for all behavior_keys across all patterns in 14-day window
   - Builds completion map keyed by behavior_key → date → completed
   - For each pattern, computes daily ratio: (completed steps) / (total steps in pattern)
   - Trend derived from comparing first-half and second-half 7-day averages (±0.1 threshold)
   - Efficient: O(1) DB queries regardless of pattern count

3. **Grow page sparkline integration** (`app/grow/page.tsx`):
   - Sparkline data loaded after patterns load (auth required, graceful fallback)
   - `sparklines` Map<patternId, SparklineData> passed through PatternSection → PatternCard
   - Trend label in validation footer: "momentum" (sage-700) for rising, "something changed" (amber-600) for dropping, nothing for stable
   - Non-stable pattern trends added to tabContext for chat overlay awareness

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 3 Session 1: "Sparkline computation." Before you can see a sparkline, you need the data behind it. This session computes 14 days of daily completion ratios for every pattern — one efficient query for all patterns, not N+1. Each day asks a simple question: of this pattern's behaviors, how many did the operator complete? The answer is a number between 0 and 1. Fourteen of those numbers, in order, is a sparkline. The trend detection compares the first week to the second week and names what it sees: rising (momentum building), stable (consistency), or dropping (something changed in the operator's context). The words matter — "something changed" is HUMA's voice, looking at the system not the person.

## Previous Session (Session 35 -- 2026-04-03)

### What Changed

1. **Deterministic insight scheduling** (`api/cron/morning-sheet/route.ts`):
   - New `shouldDeliverInsight(userId, dateStr)` function — simple string hash, mod 7, check < 2
   - Same user+date always produces the same result (~28% probability, averaging ~2 days/week)
   - Salt string `":insight"` appended to prevent correlation with other hash-based scheduling

2. **Insight delivery in morning cron** (`api/cron/morning-sheet/route.ts`):
   - After sheet compilation, checks: is today an insight day? Does user have 7+ unique dates of history?
   - If yes: first checks for existing undelivered insight in DB via `getUndeliveredInsight()`
   - If undelivered insight found: marks it delivered and uses it
   - If no undelivered insight: fetches 14-day extended history, builds `behaviorMeta` from aspirations, calls `/api/insight` for correlation-based generation
   - Generated insight saved to `insights` table with `delivered: true` and `delivered_at` timestamp
   - Insight failure is non-fatal — sheet push always goes out regardless

3. **Enhanced push notification body**:
   - Normal day: `"Your day: [headline]"`
   - Insight day: `"Your day: [headline]\n\n[first sentence of insight]."`
   - Only the first sentence (the observation) included in push — full insight saved to DB for `/today` display
   - Push tag unchanged (`morning-sheet-YYYY-MM-DD`) so one notification per day

4. **Cron response stats**:
   - Response now includes `insights` count alongside `sent` and `skipped`
   - Example: `{ date, users: 42, sent: 38, skipped: 4, insights: 8 }`

5. **saveInsight() respects delivered state** (`lib/supabase-v2.ts`):
   - Previously hardcoded `delivered: false` — now uses `insight.delivered ?? false`
   - If `insight.deliveredAt` is set, passes `delivered_at` to DB insert
   - Backwards compatible: existing callers that don't set `delivered` still get `false`

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 2 Session 5 (final): "Insight delivery via push." The Return Loop is now complete — not just a sheet delivery, but an intelligence delivery. ~2 mornings a week, for operators with enough history, the push notification carries something extra: a behavioral connection the operator didn't ask for but their data reveals. "Your morning walk and evening cooking moved together 85% of the time." Just the first sentence — the observation — because a push notification is a tap on the shoulder, not a lecture. The full insight waits on `/today`. The scheduling is deterministic (same user+date = same answer), not random, so it's testable and predictable. And the system checks for undelivered insights first before generating new ones, so existing insights from other surfaces get delivered rather than orphaned. The Return Loop now has three layers: sheet (always), insight (sometimes), reflection (evening). The system breathes in, and occasionally it breathes out something the operator didn't expect.

## Previous Session (Session 34 -- 2026-04-03)

### What Changed

1. **NotificationPreferences type** (`types/v2.ts`):
   - New `NotificationPreferences` interface: `morning` and `evening` each with `enabled` (boolean) and `hour` (0-23, local time)
   - Optional `timezone` field (IANA string, e.g. "America/New_York") — auto-detected on save
   - Added `notifications?: NotificationPreferences` to `KnownContext` interface

2. **NotificationSettings component** (`components/NotificationSettings.tsx`):
   - Half-sheet overlay following same visual pattern as EveningReflection (slide-up, backdrop, drag handle)
   - Title: "When HUMA shows up" — HUMA voice, no emoji, no exclamation marks
   - Two sections: "Morning sheet" and "Evening check-in" — each with toggle switch and hour selector
   - Toggle switches: sage-700 when on, sand-300 when off, smooth 200ms HUMA easing transitions
   - Hour pills: morning 5am-10am, evening 5pm-10pm — sage fill on selected, sand border on unselected
   - Subtitle copy in HUMA voice: "Your day, compiled fresh" / "How today landed"
   - Loads current preferences from `known_context.notifications` on open
   - Saves to Supabase via `updateKnownContext()` with shallow merge — auto-detects timezone via `Intl.DateTimeFormat`
   - "Saved" confirmation state with 800ms auto-close
   - z-index 88 (between NotificationPrompt at 90 and EveningReflection at 85)

3. **Today page integration** (`app/today/page.tsx`):
   - "Notification settings" underlined text link appears below sheet content, only when push state is "subscribed"
   - Subtle: 12px, ink-500 (#8A7D6B), sand underline — information link, not a call to action
   - Opens NotificationSettings half-sheet
   - NotificationSettings rendered inside TabShell alongside AspirationQuickLook

4. **Morning cron respects preferences** (`api/cron/morning-sheet/route.ts`):
   - After fetching known_context for each user, checks `notifications.morning.enabled`
   - If explicitly `false`, skips the user (increments `totalSkipped`) — default behavior remains enabled
   - No breaking change: users without preferences receive notifications as before

5. **Evening reflection respects preferences** (`components/EveningReflection.tsx`):
   - Now loads notification preferences from Supabase before showing
   - If `notifications.evening.enabled === false`, reflection overlay never appears
   - If custom `evening.hour` is set, uses that instead of hardcoded 17 (5pm)
   - Gracefully falls back to default 5pm if Supabase unavailable or no preferences saved
   - Effect is now async to accommodate the preferences fetch

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 2 Session 4: "Notification preferences." The Return Loop has both ends — morning push out, evening reflection in. But without preferences, HUMA decides when to show up. That violates sovereignty. This session gives the operator control over timing and opt-out. Morning sheet at 7am or 9am. Evening check-in at 9pm or 10pm. Or neither. The preferences live in `known_context.notifications` — the same context object that the sheet compiler and reflection engine already read — so the system uses one source of truth. The settings surface is a single underlined link on the Today page, not a settings gear, not a hamburger menu. HUMA doesn't have "settings" — it has preferences the operator tells it about. The voice in the half-sheet matches: "When HUMA shows up" not "Configure your notifications."

## Previous Session (Session 33 -- 2026-04-03)

### What Changed

1. **Evening reflection overlay** (`components/EveningReflection.tsx`):
   - Half-sheet bottom overlay — reuses the ChatSheet visual pattern (slide-up, backdrop, drag handle)
   - Shows after 5pm local time, only if operator has 1+ check-offs in today's sheet
   - Once per day (flagged in localStorage: `huma-v2-reflection-YYYY-MM-DD`)
   - 4-second delayed entrance to avoid competing with page load
   - Three tap options in HUMA voice: "Solid day." / "Something shifted." / "Hard one."
   - Single textarea input with context-aware placeholder: "What changed?" for shifted, "What pulled the weight down?" for hard, "Anything to add?" for solid
   - Selected state: sage-700 fill, unselected: sand border outline
   - After submit, shows Claude's 1-2 sentence response, then "Good night" dismiss button
   - "Not now" dismisses without marking as reflected (can reappear on revisit)
   - Exit animation: 280ms slide-down + backdrop fade-out

2. **Reflection API endpoint** (`api/reflection/route.ts`):
   - POST: takes `type` (solid/shifted/hard), optional `text`, and `todaysSheet` (sheet headlines with check states)
   - Sends to Claude (Sonnet) with existing known_context + today's sheet for reference
   - Prompt demands HUMA voice: fence-post neighbor, 1-2 sentences max, no flattery, no cheerleading
   - Claude returns JSON: `response` (string) + `context_updates` (object or null)
   - If context_updates is non-null, shallow-merges into known_context via `updateKnownContext()`
   - Saves both user reflection and HUMA response as chat_messages for conversation history
   - Rate-limited, auth required

3. **TabShell integration** (`components/TabShell.tsx`):
   - EveningReflection rendered between NotificationPrompt and ChatBubble (z-index 85)
   - Available on all tab pages (Today, Whole, Grow)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (24 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 2 Session 3: "Evening reflection overlay." The morning push (Session 32) starts the operator's day with a specific sheet. The evening reflection closes it. Not a journal — not "how did that make you feel?" Three options that match the range of a real day: solid, shifted, hard. The text input is optional because sometimes tapping "hard one" is all the signal the system needs. The response flows into known_context, so tomorrow's sheet is informed by tonight's reflection. The Return Loop now has both ends: morning push out, evening reflection in. The system breathes.

## Previous Session (Session 32 -- 2026-04-03)

### What Changed

1. **Supabase admin client** (`lib/supabase-admin.ts`):
   - Service-role client for server-side operations that bypass RLS
   - Used by cron jobs that run without user cookies
   - Requires `SUPABASE_SERVICE_ROLE_KEY` env var

2. **Morning sheet cron endpoint** (`api/cron/morning-sheet/route.ts`):
   - GET handler authenticated via `CRON_SECRET` (Vercel sends `Authorization: Bearer <secret>`)
   - Queries all users with push subscriptions (deduplicates multi-device users)
   - For each user: fetches aspirations, known_context, WHY statement, archetypes, 7-day check-off history, last 20 conversation messages from Supabase
   - Calls `/api/sheet` internally with full enriched context (reuses all prompt logic, scoring, cap enforcement)
   - Picks trigger item: first morning entry, or first entry overall
   - Sends push notification: title "HUMA", body "Your day: [headline]", tap opens `/today`
   - Tag `morning-sheet-YYYY-MM-DD` prevents duplicate notifications
   - 5-minute maxDuration for processing all subscribers
   - Returns JSON summary: `{ date, users, sent, skipped }`

3. **Push utility refactored** (`lib/push-send.ts`):
   - `sendPushToUser()` now accepts optional `externalSupabase` parameter
   - Cron passes admin client; existing callers (future sessions) keep cookie-based default
   - `PushPayload` type exported for reuse

4. **Sheet API cron bypass** (`api/sheet/route.ts`):
   - Rate limiting now checks for `CRON_SECRET` in Authorization header
   - Internal cron calls skip IP-based rate limiting
   - Normal client requests still rate-limited as before

5. **Vercel cron configuration** (`vercel.json`):
   - Schedule: `0 12 * * *` (daily at 12:00 UTC — morning for US timezones)
   - Path: `/api/cron/morning-sheet`

6. **Environment variables** (`.env.example`):
   - `SUPABASE_SERVICE_ROLE_KEY` — server-side admin access for cron
   - `CRON_SECRET` — Vercel cron authentication

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (23 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 2 Session 2: "Morning push notification." Session 31 built the plumbing — service worker, subscription storage, permission prompt. This session turns it on. Every morning at 12:00 UTC, Vercel fires the cron. For each operator who said "Sure" to notifications, HUMA compiles a fresh sheet using their full context — aspirations, check-off history, conversation, archetypes, WHY — and delivers one push notification. Not a generic reminder. Not "Time to check your tasks!" The operator's phone lights up with: "Your day: Stew night — chuck from freezer." Their specific day, their specific life. Tap opens `/today` with the full sheet waiting. The Return Loop is live.

## Previous Session (Session 31 -- 2026-04-03)

### What Changed

1. **Service worker** (`public/sw.js`):
   - Registers on page load via `usePush` hook
   - Handles `push` events: parses JSON payload (title, body, url, tag), shows native notification
   - Handles `notificationclick`: focuses existing HUMA tab and navigates to payload URL, or opens new tab

2. **PWA manifest** (`public/manifest.json`):
   - Name: HUMA, start_url: /today, display: standalone
   - Sand-50 background, sage-700 theme color
   - Linked from root layout via `metadata.manifest`

3. **Push subscription hook** (`lib/use-push.ts`):
   - `usePush(userId)` — returns `{ state, subscribe }`
   - States: unsupported → prompt → subscribing → subscribed (or denied)
   - Registers SW, checks existing subscription, manages permission flow
   - On subscribe: requests permission, creates PushManager subscription with VAPID key, POSTs to `/api/push/subscribe`
   - `urlBase64ToUint8Array` helper for VAPID applicationServerKey conversion

4. **Subscribe API route** (`api/push/subscribe/route.ts`):
   - POST: validates auth (user.id must match body.userId), upserts push subscription to Supabase
   - Stores endpoint, p256dh, auth keys — one row per device per user
   - Conflict resolution on (user_id, endpoint) so re-subscribing updates rather than duplicates

5. **Server-side push utility** (`lib/push-send.ts`):
   - `sendPushToUser(userId, payload)` — sends push to all user's subscribed devices
   - Auto-cleans expired subscriptions (404/410 responses)
   - Configured via VAPID env vars, graceful no-op if unconfigured

6. **Supabase migration** (`011_push_subscriptions.sql`):
   - `push_subscriptions` table: id, user_id, endpoint, p256dh, auth, created_at, updated_at
   - RLS policies: users can only manage their own subscriptions
   - Unique constraint on (user_id, endpoint)

7. **NotificationPrompt component** (`components/NotificationPrompt.tsx`):
   - HUMA-voiced: "Your sheet builds fresh each morning. Want a heads-up when it's ready?"
   - Shows only after 2+ days of use, only when permission is "prompt" (never nagged)
   - "Sure" button triggers subscribe flow; "Not now" dismisses permanently (localStorage flag)
   - Sand-50 card with sage CTA, 3s delayed entrance, HUMA easing animation
   - Rendered inside TabShell on all tab pages

8. **Environment variables**:
   - `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (client), `VAPID_PRIVATE_KEY` (server), `VAPID_SUBJECT` (server)

### Dependencies Added
- `web-push` — server-side Web Push protocol (VAPID signing, push delivery)
- `@types/web-push` — TypeScript declarations

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (22 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 2 Session 1: "Service worker + web push setup." The plumbing for the Return Loop — SW registration, VAPID keys, subscription storage, permission UX.

## Previous Session (Session 30 -- 2026-04-03)

### What Changed

1. **Behavior leverage scoring** (`api/sheet/route.ts`):
   - New `scoreBehaviors()` function scores each behavior by: check-off momentum (sticking vs. struggling), day-of-week scheduling fit (specific-days behaviors scheduled for today get +3), daily frequency bonus, and new-behavior priority
   - Aspiration balance: no single aspiration can take more than 3 of the top 7 candidates, ensuring the sheet reflects the operator's full life, not just one aspiration
   - Scored behaviors marked with `★ PRIORITY` or `(lower priority today)` tags in the prompt so Claude knows which to favor

2. **Selection guidance in prompt** (`api/sheet/route.ts`):
   - When total behaviors > 5, a new `SELECTION GUIDANCE` section is injected explaining the pre-scoring rationale (momentum, scheduling, dimensional balance)
   - Claude instructed to choose from priority behaviors first, with lower-priority only if clearly better for today

3. **Hard 5-entry cap — three layers** (`api/sheet/route.ts`, `lib/sheet-compiler.ts`, `app/today/page.tsx`):
   - Server: `parsed.entries.slice(0, 5)` before returning JSON
   - Client helper: `rawEntries.slice(0, 5)` in `compileSheet()`
   - Today page: `.slice(0, 5)` on both cached entries and fresh compilation results
   - Belt-and-suspenders: even if Claude ignores the prompt rule, the operator never sees more than 5

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes, 0 errors)

### Why This Matters (Roadmap)
Increment 1 Session 6 (final): "Sheet cap + selection logic." The sheet prompt already said "maximum 5" but nothing enforced it, and when an operator has 8+ behaviors across aspirations, Claude received all of them with equal weight. This session adds intelligence to the selection: behaviors building momentum stay on the sheet, behaviors scheduled for today get priority, behaviors not sticking get simplified, and no single aspiration dominates. The hard cap is the last piece of the "specific sheet" increment — the operator's daily sheet now reflects what actually matters today, selected from their full behavioral landscape. Five items. The right five.

## Previous Session (Session 29 -- 2026-04-03)

### What Changed

1. **Check-off gesture redesign** (`app/today/page.tsx`):
   - Removed all checkbox circles from PatternRouteCard, CompiledEntryRow, and StandaloneBehaviorRow
   - Tap the row itself to check off — no checkbox target, no "task list" aesthetic
   - Checked text shifts to ink-200 (#C4BAA8) with thin 1px line-through (decoration also ink-200)
   - Trigger badge ("THE DECISION") and caption text also fade to ink-200 when checked
   - Trigger item background clears on check (warm gradient removed)

2. **Connector line removed** (PatternRouteCard):
   - Vertical connector line between behavior steps removed entirely
   - Steps now have direct padding (16px) instead of 48px left offset for checkbox
   - Cleaner visual flow without the sequential task-list pattern

3. **Dimension dot glow preserved**:
   - On check: dots briefly glow (scale 1.8x, full opacity) via `animate-dim-glow`
   - After glow: dots settle to 35% opacity on checked items
   - On uncheck: dots return to full opacity

4. **Animation cleanup** (`globals.css`):
   - Removed `@keyframes check-bounce` and `.animate-check-bounce` (no longer needed without checkbox)
   - Removed corresponding `prefers-reduced-motion` override

### What Was Verified
- Production build: passes clean (20 routes, 0 errors)
- Toggle confirmed via dev server: ink-200 color + line-through on check, ink-900 + no decoration on uncheck

### Why This Matters (Roadmap)
Increment 1 Session 5: "Check-off gesture redesign." The checkbox is the most recognizable symbol of a task list — and HUMA is not a task list. Removing the circle and replacing it with a whole-row tap that quietly fades the text transforms the interaction from "completing a task" to "acknowledging you did the thing." The thin line-through and faded ink are the visual equivalent of a gentle nod, not a triumphant checkmark. The dimension dots glow briefly to show the ripple effect — this behavior touched Body, Money, Joy — then settle. The operator reads their sheet, taps what happened, and the page breathes.

## Previous Session (Session 28 -- 2026-04-03)

### What Changed

1. **Through-line in sheet API** (`api/sheet/route.ts`):
   - Prompt now requests a `through_line` field — a single sentence (max 20 words) naming the structural connection across today's entries
   - Good/bad examples in prompt to calibrate voice: "your evening rhythm is where Body, Money, and Joy converge" not "You have several important tasks today"
   - Response parsing extracts `through_line` from Claude's JSON and returns it alongside entries

2. **compileSheet() return type** (`lib/sheet-compiler.ts`):
   - New `CompiledSheet` interface: `{ entries: SheetEntry[]; throughLine: string | null; date: string }`
   - API response mapped from snake_case to camelCase `SheetEntry` objects inside `compileSheet()`
   - Properly typed — no raw `Record<string, unknown>` leaking to callers

3. **Today page wired to compileSheet()** (`app/today/page.tsx`):
   - New state: `compiledEntries`, `throughLine`, `sheetCompiling`
   - Sheet compilation effect runs after aspirations load, gated by `sheetCompiledRef` to prevent double-compilation
   - Checks localStorage cache first (`huma-v2-compiled-sheet-YYYY-MM-DD`); if cache hit, uses cached entries
   - On cache miss, calls `compileSheet()` with operator name, archetypes, WHY statement
   - Caches successful compilation in localStorage for same-day revisits

4. **Through-line header UI**:
   - Renders between aspiration ribbon and sheet entries: font-serif, italic, 15px, sage-600
   - Only shown when `throughLine` exists and compiled entries are present
   - Uses `animate-entrance-2` for consistent page stagger

5. **CompiledEntryRow component**:
   - Renders each compiled entry with headline (font-serif, 16px), dimension dots, and expandable detail
   - Check circle: tap to toggle, sage fill when checked, line-through on headline
   - "how ↓" hint expands detail section (font-sans, 13px, sage-500) with HUMA easing animation
   - All entries in a single white card with sand border (consistent with PatternRouteCard aesthetic)

6. **Fallback behavior**:
   - When compiled entries exist: compiled sheet shown, PatternRouteCards hidden
   - During compilation: "Building your day..." in italic serif
   - When compilation fails or returns empty: PatternRouteCards shown as before

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

### Why This Matters (Roadmap)
Increment 1 Session 3: "Wire compileSheet + through-line header." Sessions 25-26 built the data pipeline and rewrote the prompt. This session completes the circuit — the Today page now actually calls the enriched sheet compiler and renders its output. The through-line header is the first structural connection the operator sees each day: not just "here are your tasks" but "here's the thread that runs through them." This is the Quadrivium's Music layer applied to the daily sheet — number in time becomes meaning in sequence.

## Previous Session (Session 26 -- 2026-04-03)

### What Changed

1. **SHEET_PROMPT rewrite — action-level, not behavior-level** (`api/sheet/route.ts`):
   - Complete prompt rewrite: demands TODAY'S ACTIONS, not generic behaviors
   - Explicit behavior→action contrast examples: "Cook dinner" → "Stew night — chuck in freezer + Sunday broth"
   - Structured prompt sections: identity, known context, conversation transcript, aspirations, history
   - Headline rule reinforced: operator reads headline and KNOWS WHAT TO DO without tapping
   - Context usage mandate: names, budget, foods, schedule, season, check-off history must appear in output
   - Fallback rule: "If context is thin, use season + day of week + aspiration details. NEVER fall back to generic behavior names."

2. **Archetypes + WHY statement now injected into prompt**:
   - Previously gathered by client but ignored by prompt — now wired into `{identity_section}` placeholder
   - Archetypes rendered as "Archetypes: Earth Tender + Creator"
   - WHY rendered as quoted statement
   - Day count injected via `{day_count}` placeholder

3. **System message updated**:
   - Added: "specific actions, not generic behaviors" and "Every headline names what to do today, not what the behavior is"

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Previous Session (Session 25 -- 2026-04-03)

### What Changed

1. **Enriched SheetCompileRequest type** (`types/v2.ts`):
   - Expanded from 2 fields (userId, date) to 11: name, date, aspirations, knownContext, recentHistory, conversationMessages, dayOfWeek, season, dayCount, archetypes, whyStatement
   - Full `KnownContext` type used (structured, not generic Record)

2. **Season derivation + structured context formatting** (`api/sheet/route.ts`):
   - `getSeason()` — derives "early spring", "mid summer", etc. from date using solstice-approximate boundaries with early/mid/late qualifier
   - `formatKnownContext()` — renders KnownContext as readable prose (Location, Work, People, Life stage, Health, Time, Resources) instead of raw JSON dump
   - Route handler uses typed `SheetCompileRequest`, passes season via `{season}` placeholder
   - Accepts dayCount, archetypes, whyStatement (wired to body, not yet used in prompt — future session)

3. **Client-side sheet compiler** (`lib/sheet-compiler.ts`):
   - `compileSheet()` gathers: known context (Supabase → localStorage), 7-day check-off history, last 20 conversation messages, season, dayOfWeek, dayCount
   - Filters to active aspirations only
   - Assembles enriched `SheetCompileRequest` and POSTs to `/api/sheet`

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Previous Session (Session 24 -- 2026-04-03)

### What Changed

1. **WHY alignment in structural insight** (`lib/supabase-v2.ts`)
2. **Archetypes in conversation context — all tabs** (`api/v2-chat/route.ts`)
3. **Archetypes + WHY passed from Today and Grow tabs**
4. **WHY subtitle on validated pattern section** (`app/grow/page.tsx`)
5. **Archetype badge on Grow pattern cards** (`app/grow/page.tsx`)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Previous Session (Session 23 -- 2026-04-03)

### What Changed

1. **Behavioral context builder** (`api/v2-chat/route.ts`)
2. **Validation data in tabContext** (Today, Whole, Grow pages)
3. **System prompt integration** (`api/v2-chat/route.ts`)
4. **Adaptive message count** (`api/v2-chat/route.ts`)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Earlier Sessions

- **Session 22**: ContextPortrait on Whole tab (ISRU material inventory)
- **Session 21**: WeekRhythm component, day-of-week data layer, cycle disruption detection
- **Session 20**: Aggregate dimension row, aspiration provenance, tap-to-expand on Grow
- **Session 19**: Trigger caption on Today (nodal visibility), dimension pills on Grow
- **Session 18**: Dimensional visibility on Today tab (Trivium Logic layer)
- **Session 17**: Grow tab initial implementation
- **Session 16**: Mobile audit, chat context awareness, ChatSheet mobile polish
- **Session 15**: Skeleton loaders, streaming error recovery, consistent API errors, offline detection

## Data Layer: New localStorage Key

| Key | What It Stores |
|-----|---------------|
| `huma-v2-patterns` | Extracted patterns from decomposition (migrated to Supabase on auth) |
| `huma-v2-compiled-sheet-YYYY-MM-DD` | Cached compiled sheet entries + through-line for a given date |
| `huma-v2-push-prompt-dismissed` | Whether notification permission prompt was dismissed (prevents re-showing) |
| `huma-v2-reflection-YYYY-MM-DD` | Whether evening reflection was submitted today (prevents re-showing) |
| `huma-v2-dismissed-emergences` | Array of behavior_keys dismissed from emergence detection (prevents re-showing) |
| `huma-v2-dismissed-merges` | Array of "patternId:otherPatternId" strings dismissed from merge suggestions |

## Next Priority

- Run migrations `009_aspiration_phases.sql`, `010_patterns.sql`, and `011_push_subscriptions.sql` in Supabase
- Add env vars to Vercel: VAPID keys, `CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`
- **Session 41** — Grow tab breathing room: max 5-7 visible cards, fold by dimension, Alexander's Void (Increment 3, S6)
