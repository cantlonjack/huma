# HUMA Current State

_Auto-generated by CC at end of session. The strategic Claude instance reads this._
_Last updated: 2026-04-02 (Session 20)_

## Routes

| Route | Status | Notes |
|-------|--------|-------|
| `/` | Working | Landing page. Redirects authed users with aspirations to `/today`. |
| `/start` | Working | Conversation entry. Deep context gathering, reflect-back, phased decomposition. Now auto-extracts patterns from trigger behaviors. |
| `/today` | Working | Production sheet. Shows only this_week behaviors. Check-off tracking, dimension dots on every behavior row, dimension pulse on check-off, aggregate dimension summary in card footer, streak counts. Trigger caption shows dimension count + shared behavior signal. |
| `/whole` | Working | Holonic life map. Force-directed D3 visualization, archetype selector, WHY statement, insight card. |
| `/grow` | Working | Patterns view. Patterns grouped by status (finding/working/validated). Cards show trigger with dimension pills, golden pathway, aggregate dimension row ("Touches Body, Money, Joy"), validation progress, and aspiration provenance ("From: [name]"). Tap-to-expand reveals coming_up and longer_arc phases from source aspiration (one card expanded at a time, 300ms HUMA easing). Shared behavior detection. Empty state. Skeleton loader. Chat context passes pattern data. |
| `/chat` | Working | Conversation hub with grouped message cards and context summary. "See all" now links to `/whole`. |
| `/map/[id]` | Working | Living Canvas renderer with OG metadata. |
| `/map/sample` | Working | Two example canvases. |
| `/api/v2-chat` | Working | Conversation engine with adaptive prompts and [[MARKER:...]] protocol. |
| `/api/chat` | Working | Legacy multi-phase conversation for canvas generation. |
| `/api/palette` | Working | Concept chip suggestions for /start sidebar. |
| `/api/sheet` | Working | Daily production sheet compilation via Claude. |
| `/api/sheet/check` | Working | Behavior check-off persistence. Requires auth. |
| `/api/insight` | Working | Cross-aspiration behavioral insights. Requires 7+ days of data. |
| `/api/whole-compute` | Working | Archetype and WHY statement generation. |
| `/api/maps` | Working | Living Canvas storage (Supabase + Redis). |
| `/api/maps/[id]` | Working | Map retrieval (Supabase -> Redis fallback). |
| `/api/og` | Working | Dynamic OG image generation for maps. |
| `/auth/callback` | Working | Magic link auth callback. |

## Removed Routes

| Route | Removed In | Reason |
|-------|-----------|--------|
| `/system` | Session 14 | Function moves to Whole tab's expand panels (Phase 4). ConversationSheet component also removed. |

## Known Bugs

- None currently identified.
- **Migrations required:** Run `009_aspiration_phases.sql` AND `010_patterns.sql` in Supabase. Code handles missing tables/columns gracefully (patterns won't persist to DB until migration runs, but localStorage works).

## Last Session (Session 20 -- 2026-04-02)

### What Changed

1. **Aggregate dimension row on pattern cards**:
   - Below golden pathway steps, a "Touches Body, Money, Joy..." row shows ALL dimensions touched across ALL steps in the pattern (not just the trigger)
   - `getAllStepDimensions()` helper walks every step, looks up the behavior in the source aspiration, collects DimensionEffect[], deduplicates
   - 5px colored dots + 11px sage-400 dimension labels, same style as Today footer

2. **Aspiration provenance link**:
   - At the bottom of each pattern card: "From: [aspiration name]" in font-sans, 11px, italic, sage-400
   - `getAspirationName()` helper prefers `title` → `clarifiedText` → `rawText`, truncated via `displayName()`
   - Not clickable yet — provenance only

3. **Tap-to-expand detail view**:
   - Pattern cards are now tappable — tap expands to show coming_up and longer_arc phases from the source aspiration
   - `getAspirationPhases()` helper pulls `comingUp` (FutureAction[]) and `longerArc` (FuturePhase[]) from the source aspiration
   - "COMING UP" section: 9px letter-spaced label + items with name and timeframe
   - "THE LONGER ARC" section: same format with phase name and timeframe
   - Expand/collapse animated with HUMA easing (cubic-bezier(0.22, 1, 0.36, 1), 300ms) via max-height + opacity transition
   - Only one card expanded at a time (accordion pattern) — `expandedId` state managed at page level
   - If no coming_up/longer_arc data exists, card is not tappable (no expand content)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)
- Visual verification: pattern cards render with aggregate dimensions, aspiration provenance, and hidden expand sections (confirmed max-height:0 on collapsed cards)

### Why This Matters (Lineage)
Perkins' enterprise one-pager principle: every pattern should show its full context at a glance — inputs, outputs, capital impacts, timeline, and provenance. Previously pattern cards showed trigger + pathway + validation only. Now the operator sees: which dimensions the entire pattern touches (not just the trigger), where the pattern came from (aspiration link), and what's coming next (coming_up/longer_arc). This is the minimum viable Perkins Standard for pattern cards.

## Previous Session (Session 19 -- 2026-04-02)

### What Changed

1. **Trigger caption on Today tab (Nodal visibility)**:
   - Below "THE DECISION" badge + trigger text, a one-line italic caption now shows WHY it's the leverage point
   - `triggerCaption()` helper computes: how many dimensions the trigger touches (e.g. "Serves body, joy, growth"), and whether the trigger behavior appears in other aspirations (e.g. "Shared with Financial clarity")
   - Caption uses font-sans, 11px, italic, sage-400 — subtle, doesn't compete with the behavior text
   - Fades with the row when checked (inherits checked-state color transition)
   - `PatternRouteCard` now receives `allAspirations` prop for cross-aspiration detection

2. **Dimension pills on Grow tab pattern cards (Nodal visibility)**:
   - Below trigger text in each pattern card, small dimension pills show which dimensions the trigger serves
   - Uses canonical `DIMENSION_COLORS` from `types/v2.ts` — 5px colored dots + dimension label in 10px sage-400
   - `getTriggerDimensions()` helper looks up the aspiration to find the trigger behavior's dimension effects
   - `getSharedCaption()` helper detects when trigger text appears across multiple aspirations — surfaces "Shared with [name]" or "Shared across N patterns"
   - `PatternCard` and `PatternSection` now receive `aspirations` prop for lookups

3. **Shared behavior detection (cross-aspiration)**:
   - Both Today and Grow tabs detect when a trigger behavior text matches behaviors in other aspirations
   - This is Sanford's nodal signal — the single intervention that cascades across the nested system
   - On Today: shown in the trigger caption line
   - On Grow: shown as italic text below dimension pills

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)
- Visual verification: trigger captions render on Today with dimension names + shared signals; dimension pills render on Grow with colored dots and labels

### Why This Matters (Lineage)
Sanford's Nodal Principle: find the single intervention that cascades positively through the entire nested system. Previously, we showed "THE DECISION" but didn't show the operator WHY it's the leverage point. Now the operator sees: which dimensions this one behavior serves, and whether it's shared across multiple patterns — making the nodal nature visible. This is the minimum viable Nodal Visibility layer.

## Previous Session (Session 18 -- 2026-04-02)

### What Changed

1. **Dimensional visibility on Today tab (Trivium Logic layer)**:
   - Every behavior row in PatternRouteCard now shows colored dimension dots (right-aligned) indicating which of the 8 dimensions that behavior touches
   - `BehaviorStep` type extended with `dimensions: string[]` (was single `dimension: string`)
   - `getBehaviorChain` now extracts all dimensions from `Behavior.dimensions[]` (handles both string and `DimensionEffect` formats)
   - Dimension dots pulse/glow animation (`animate-dim-glow`) on check-off — draws operator attention to which dimensions they're building
   - Dots fade to 40% opacity when behavior is checked (follows existing checked-state visual language)
   - Card footer now shows aggregate dimension summary: colored dots + "Touches Body, Money, Joy, Home, Growth" text
   - Footer layout adjusted: dimension summary row above timing/validation strip
   - New CSS keyframe `dim-dot-glow` (500ms scale+opacity, HUMA easing curve) with `prefers-reduced-motion` support
   - Behavior rows changed from `display: block` to `display: flex` for proper dot alignment

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)
- Visual verification: dimension dots render correctly on all behavior rows, footer shows aggregate dimensions

### Why This Matters (Lineage)
This is the minimum viable Logic layer from the Trivium architecture. Previously the operator checked off behaviors without seeing which dimensions they were building — Grammar (conversation) and Rhetoric (production sheet) worked, but Logic (connections) was invisible. Now every check-off is a moment of dimensional awareness, supporting the graduation metric (operator developing capacity to see their life as a connected system).

## Previous Session (Session 17 -- 2026-04-02)

### What Changed

1. **Grow tab initial implementation**:
   - Pattern cards showing trigger (The Decision), golden pathway steps, time window, and validation progress bar
   - Patterns grouped by status: Validated, Working, Finding — each with section header + subtitle
   - Data loading: Supabase → localStorage fallback → extract from aspirations fallback
   - Empty state with seed icon and "Patterns emerge from living" messaging
   - Skeleton loader (`GrowSkeleton`) matching card layout
   - Chat context awareness: Grow tab passes pattern names, triggers, statuses, and validation counts to conversation engine
   - v2-chat API: Grow tab context block updated with pattern-aware tone and pattern list

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Previous Session (Session 16 -- 2026-04-02)

### What Changed

1. **Mobile audit — touch targets & safe areas**:
   - Added `viewport-fit: cover` via Next.js `Viewport` export in `layout.tsx` (enables `env(safe-area-inset-*)` on iOS)
   - BottomNav: safe-area padding now extends total height (not inside fixed 56px)
   - BottomNav links: explicit `minHeight: 44px` touch targets
   - ChatSheet close button: 28px → 44px touch target
   - ChatSheet drag handle: 44px min-height touch area
   - Today aspiration ribbon pills: 44px min-height, larger padding (6px→10px)
   - Today QuickLook drag handle: 44px min-height
   - Today insight card buttons ("Tell me more", dismiss): 44px min-height
   - Today "Adjust route" button: 44px min-height

2. **Mobile audit — scroll/overlap fixes**:
   - TabShell: `hideBubble` prop hides ChatBubble when page has its own prompt bar (Today)
   - Prevents ChatBubble/prompt bar overlap on Today tab

3. **Mobile audit — text truncation**:
   - PatternRouteCard header: `text-overflow: ellipsis` on aspiration names
   - Empty-state card header: same treatment
   - RerouteCard: name truncated with `maxWidth: 60%`
   - QuickLook title: `word-break: break-word` for long names

4. **Chat context awareness**:
   - TabShell: new `sourceTab` ("today"|"whole"|"grow") and `tabContext` props
   - ChatSheet: forwards `sourceTab` + `tabContext` to `/api/v2-chat`
   - v2-chat API: `buildTabContextBlock()` generates tab-specific system prompt additions:
     - **Today**: operational tone, lists active patterns with behaviors
     - **Whole**: reflective tone, includes archetypes, WHY statement, principles
     - **Grow**: forward-looking tone
   - Today page passes aspiration names, behaviors, statuses as tabContext
   - Whole page passes archetypes, WHY, principles as tabContext

5. **ChatSheet mobile polish**:
   - `visualViewport` resize listener: detects mobile keyboard and adjusts sheet bottom/height
   - Smart auto-scroll: only scrolls to bottom if user was already near bottom (within 80px)
   - Velocity-aware drag-to-dismiss: fast swipe (>0.5px/ms) dismisses even with short distance
   - Smooth transition on drag release (200ms ease)
   - Input font bumped to 16px (prevents iOS auto-zoom on focus)
   - `enterKeyHint="send"` for mobile keyboard send button
   - Safe-area bottom padding removed when keyboard is open (avoids double padding)

### What Was Verified
- TypeScript: 0 errors
- Production build: passes clean (20 routes)

## Earlier Sessions

- **Session 15**: Skeleton loaders, streaming error recovery, consistent API errors, offline detection
- **Session 16**: Mobile audit (touch targets, safe areas, scroll/overlap, text truncation), chat context awareness, ChatSheet mobile polish

## Data Layer: New localStorage Key

| Key | What It Stores |
|-----|---------------|
| `huma-v2-patterns` | Extracted patterns from decomposition (migrated to Supabase on auth) |

## Next Priority

- Run migrations `009_aspiration_phases.sql` and `010_patterns.sql` in Supabase
- Session 21: Temporal pattern surface (weekly rhythm heatmap — the missing Quadrivium "Music")
- Session 22: Context surface on Whole tab (ISRU — operator sees their materials)
- Session 23: Behavioral data feedback to conversation (close the error-correction loop)
- Session 24: WHY & archetype integration (connect identity to the rest of the system)
